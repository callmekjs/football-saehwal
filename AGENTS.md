# 이 저장소에서 작업하는 규칙

> 이 프로젝트는 **여러 AI 에이전트가 같은 폴더를 공유**한다. 서로 고친 것이 섞이지 않으려면 아래를 지켜야 한다.
> 사람 사용자는 **비전공자**다. 설명은 전문용어 없이 쓴다.

**마감 2026-08-03(월) 10:00. 그 이후 커밋 이력이 있으면 실격이다.** 실질 마감은 8/2(일) 밤.

---

## 0. 공유 작업 규칙 — 가장 먼저 읽을 것

1. **작업을 시작하기 전에 `git pull` 하고, 끝나면 즉시 커밋·푸시한다.** 작업 트리를 오래 더럽혀 두지 않는다.
2. **커밋하지 않은 변경 위에 다른 작업을 쌓지 않는다.** 다른 에이전트가 그 사이에 손댔을 수 있다.
3. 시작 전 `git status`로 남의 미완성 작업이 있는지 본다. 있으면 건드리지 말고 사용자에게 알린다.
4. **커밋 메시지는 아래 형식을 지킨다.** 에이전트가 넷이고 Codex와 Claude Code 양쪽에서 커밋하므로, 형식이 갈리면 이력이 읽히지 않는다. 제출물에 GitHub 링크가 포함되어 **커밋 이력 자체가 평가 대상**이다.
5. **`docs/progress.md`의 진행 표시를 갱신한다.** 사용자가 이 파일 하나로 위치를 파악한다.
6. **`docs/HANDOFF.md`의 "지금까지 끝난 것 / 다음에 할 것"도 함께 갱신한다.** 이 파일이 다른 에이전트에게 넘기는 인수인계서다. 갱신하지 않으면 다음 사람이 이미 끝난 일을 다시 한다.

### 커밋 메시지 형식

```
type: 제목

본문. 무엇을 왜 바꿨는지 쓴다.
```

- 제목과 본문은 **빈 줄 하나**로 분리한다
- 제목은 영문 기준 **50자 이내** (한국어면 대략 25자 안쪽), **끝에 마침표 금지**, **명령문**
- 본문은 영문 기준 **72자마다** 줄바꿈
- 본문은 **어떻게가 아니라 무엇을·왜**에 맞춰 쓴다. 무엇을 바꿨는지는 diff에 있으므로 **왜**에 지면을 쓴다
- 제목은 **한국어**, 유형 접두사는 **영문**이다

| 유형 | 쓰는 때 |
|---|---|
| `feat` | 새로운 기능 추가 |
| `fix` | 버그 수정 |
| `docs` | 문서 수정 |
| `style` | 코드 자체 변경이 없는 포맷팅 |
| `refactor` | 리팩토링 |
| `test` | 테스트 코드 추가·수정 |
| `chore` | 빌드·패키지·설정 (`.gitignore` 등) |

예: `feat: 전술 프리셋 다섯 종 추가` · `fix: 종료 뒤에도 연출이 계속되던 것`

---

## 1. 절대 어기면 안 되는 것

### `src/sim/` 은 브라우저를 모른다

`window` · `document` · `requestAnimationFrame` · `localStorage` · `Date` · `Math.random` 을 **일절 참조하지 않는다.**

같은 코드가 브라우저(게임)와 Node(자동 검증)에서 동일하게 돌아야 한다. 이게 깨지면 `npm run sim`과 경기 분석 화면이 **동시에** 죽는다.

확인:
```bash
npx tsx -e "import('./src/sim/engine.ts').then(()=>console.log('OK'))"
```

### 난수 소비 순서는 고정이다

`src/sim/attack.ts`의 `drawTick()`은 매 틱 정확히 18개를 정해진 순서로 뽑는다. **필요 없는 슬롯도 반드시 뽑아서 버린다.**

조건 분기 안에서 난수를 뽑으면 분기가 갈릴 때마다 이후 수열이 밀려 재현이 조용히 깨진다. 증상이 눈에 안 띄어서 마감 직전에 발견되는 종류의 고장이다.

**슬롯을 추가하거나 순서를 바꾸면 저장된 모든 시드의 결과가 달라진다.** 밸런스를 처음부터 다시 재야 한다.

### 실존 선수의 이름을 넣지 않는다

이 게임의 실점 공식은 "우리 수비수 중 가장 느린 선수"를 실점 원인으로 **기계적으로 지목**한다. 실명이 붙으면 "이 선수 때문에 졌다"가 자동 생산되어 대회 규칙(특정 선수 비하 금지)에 걸린다.

`Player` 타입에 `name` 칸 자체가 없다. 테스트로도 막아뒀다(`squad.test.ts`). 선수 사진·엠블럼·유니폼 이미지 파일도 0개다.

### 확률 숫자는 `src/sim/constants.ts` 에만 둔다

다른 파일에 숫자 리터럴을 흩뿌리면 8/1 동결 이후 어디를 손댔는지 추적할 수 없다. 포메이션 계수만 예외로 `formations.ts`에 있다.

### 국면 추가에 코드 수정이 없어야 한다

국면은 `src/data/problems.json`의 한 덩어리다. 새 국면을 넣으려고 엔진을 고쳐야 한다면 설계가 잘못된 것이다.

---

## 2. 바꾸기 전에 확인해야 하는 것

```bash
npm test        # 289개. 전부 통과해야 한다
npx tsc --noEmit # 타입 오류 0
npm run sim     # 세 국면 전부 "합격"
```

`npm run sim`이 "미달"을 내면 **`src/sim/constants.ts`만 조정한다.** 구조를 바꾸지 않는다.

### 밸런스 합격 기준

| 기준 | 뜻 |
|---|---|
| 무개입 통과율 **50% 이하** | 아무것도 안 하면 대체로 무너져야 퍼즐이다 |
| 최선과의 격차 **20%p 이상** | 제대로 하면 확실히 나아야 한다 |

미달인 국면은 튜닝하지 말고 **목록에서 뺀다.** 국면은 코드가 아니라 데이터라 빼는 비용이 0이다.

### 테스트에 조정 대상 숫자를 박지 말 것

`expect(c.setPiece).toBeCloseTo(2.8)` 같은 테스트는 밸런싱할 때마다 깨진다. **방향과 비율**로 쓴다.

```ts
expect(low.setPiece).toBeGreaterThan(mid.setPiece * 2)   // 좋다
expect(low.setPiece).toBeCloseTo(4.2)                    // 나쁘다
```

---

## 3. 이 게임이 성립하는 이유 — 건드리면 무너지는 세 가지

### `initialTactics` · `initialFormation` — 앞 감독이 잘못 걸어놓은 지시

중립 상태에서 시작하면 **아무것도 안 해도 통과한다.** 실측으로 무개입 통과율이 77%까지 올라갔었다. 모든 국면은 잘못 걸린 상태를 물려받고, 플레이어의 첫 과제는 그걸 되돌리는 것이다.

### 라인 낮음의 세트피스 승수

`LINE[0].setPiece`가 배후 침투 감소(`×0.45`)를 **압도해야** 한다. 그래야 "깊이 잠글수록 오히려 더 맞는다"가 성립한다. 이게 깨지면 라인 낮음이 전 국면 지배 전략이 되어 국면 2·4의 반전이 원리적으로 불가능해진다. `constants.test.ts`가 이 부등식을 지킨다.

### 폭의 수비 쪽 대가 (`WIDTH.*.oppOpen`)

폭을 벌리면 공격 기회가 늘지만 **중앙이 열려야** 한다. 이 대가가 없으면 넓게가 공짜가 되어 전 국면에서 정답이 되고, **만능 조합이 생겨 "판마다 다른 판단이 필요하다"가 무너진다.** 실제로 한 번 그렇게 됐다가 고쳤다.

### 순서가 실재하는 것은 레버가 아니라 교체 카드다

레버는 확률에 **곱해지는 승수**다. 곱셈에는 순서가 없다(`2×3×5` = `5×2×3`). 레버로 순서 퍼즐을 만들려는 시도는 원리적으로 실패한다. 순서는 카드에서 나온다 — 유한하고, 회수 불가이며, 반영에 6초가 걸린다.

---

## 4. 화면 쪽 규칙

- **데스크톱 브라우저가 기본이다.** 사용자가 정했다 — *"우리는 동적웹을 만드는거야 그러니까 핸드폰용으로 화면을 만들 필요 없어."* 대회 규칙도 "주요 브라우저 환경에서 정상 실행"을 요구할 뿐 폰을 요구하지 않는다.
  - **폰 전용 화면을 따로 설계하지 않는다.** 375px 최적화에 시간을 쓰지 않는다.
  - 다만 **폰에서 깨지지는 않아야 한다.** 가로 넘침 0, 버튼이 손가락에 닿을 것. 1차 관문이 대중 투표라 폰으로 여는 사람이 섞인다. "따로 만들지 않는다"와 "깨져도 된다"는 다르다.
  - **넓은 화면을 실제로 쓴다.** 경기장이 가장 큰 면적을 차지해야 하고, 화면 아래쪽이 통째로 비면 실패다.
- 모든 버튼 최소 높이 **44px**.

### 드래그 — 어디는 되고 어디는 안 되는가

전에는 드래그를 통째로 금지했다. 그건 **폰이 기준일 때** 만든 규칙이고, 지금은 데스크톱이 기준이다. 사용자가 정했다 — *"포지션은 사용자가 마우스로 드레그해서 포지션 변화를 이끌 수 있게 해줘."* 대회 개요도 **"직접 드래그, 클릭, 배치, 조작하며"** 라고 적고 있다.

- **패널 안의 선수 카드는 드래그해도 된다.** 44px 이상이라 마우스로 잡힌다.
- **경기장 캔버스 위의 선수 원은 여전히 안 된다.** 반지름이 7픽셀이라 표적이 못 된다. 이건 화면 크기 문제가 아니라 물리적 크기 문제라 데스크톱에서도 같다.
- **드래그로 되는 것은 탭으로도 반드시 돼야 한다.** 터치 기기와 마우스가 없는 환경이 있고, 규칙상 작동하지 않는 인터랙션은 평가에서 제외된다. 드래그는 **빠른 길**이지 유일한 길이 아니다.
- 교체는 계속 **두 단계 탭**이다. 카드가 유한하고 회수 불가라 실수하면 되돌릴 수 없다.

### 급수 타임 — 킥오프 전 화면의 설정

킥오프 전 화면은 설정 메뉴가 아니라 **경기 안의 한 장면**이다. 사용자가 정했다 — *"지금 hydration break에 있고 감독은 현재 상황을 타파해야 해, 그러니까 충분히 플레이 전 선수들에게 지시를 할 수 있어."*

- 급수 타임 동안 **포메이션 · 전술 · 선수 지시 · 교체를 모두 걸 수 있다.** 시계는 멈춰 있다.
- 휘슬이 울리면 **75초 동안 멈추지 않는다.** 그 뒤로는 되돌릴 수 없다.
- 이 설정이 있어야 "왜 경기 전에 다 만질 수 있는가"와 "왜 시작 뒤에는 시계가 안 멈추는가"가 함께 설명된다. 그리고 75초를 조작이 아니라 **경기를 보고 대응하는 데** 쓸 수 있다.
- 경기 진행은 **절대 시각 기준**이다(`useMatch.ts`). `requestAnimationFrame`은 탭이 화면에 없으면 멈추는데, 그걸 쓰면 탭 전환이 일시정지 치트가 되어 "시계는 멈추지 않는다"는 전제가 무너진다.
- 화면에만 쓰이는 값(`ball`, `stats`)은 **이미 뽑아둔 난수에서 파생**한다. 난수를 추가로 소비하면 경기 결과가 바뀐다.
- 보기만 바뀌고 확률에 영향이 없는 조작은 넣지 않는다.

---

## 4.4 Codex ↔ Claude Code 에이전트 공통 규칙

앞으로 새 전문 에이전트를 만들 때 역할 지침의 단일 원본은
`docs/agents/<이름>.md`에 둔다. Codex의 `.codex/agents/*.toml`과 Claude
Code의 `.claude/agents/*.md`는 각 플랫폼이 에이전트를 찾는 실행 파일이며,
작업 시작 전에 같은 공통 원본을 끝까지 읽도록 만든다.

한 플랫폼에 전문 에이전트를 추가하면 같은 커밋에서 다른 플랫폼의 실행
파일도 함께 추가한다. 역할·범위·금지선·검증 기준을 두 실행 파일에 복사하지
않는다. 그래야 한쪽만 수정되어 서로 다른 에이전트가 되는 일을 막을 수 있다.

---

## 4.5 게임 전문가 에이전트

축구 룰과 게임 시스템은 **`game-agent`** 가 관리한다 (`.claude/agents/game-agent.md`).

이 에이전트에게 맡기는 일:
- **경기 규칙·사실성 검토** — "이거 축구 맞아?"
- 시뮬레이션 엔진·확률 모델 수정 (`src/sim/`)
- 관전 연출 수정 — 선수 움직임·패스·슛·태클 (`src/render/`)
- 밸런스 측정과 튜닝, 국면 데이터 추가

맡기지 않는 일: UI 레이아웃, 배포, 문서.

정의 파일에는 **축구 룰 체크리스트**와 **이 프로젝트에서 확립된 사실성 기준**(패스 성공률, 압박 게이지, 선수 속도 등)이 들어 있다. 관전 품질을 손볼 때는 그 문서를 기준으로 삼는다.

## 4.6 UX/UI 전문가 에이전트

화면 정보 순서와 웹 조작·반응형 품질은 UX/UI 전문 에이전트가 관리한다.
Codex에서는 **`uxui_agent`** (`.codex/agents/uxui-agent.toml`), Claude
Code에서는 **`uxui-agent`** (`.claude/agents/uxui-agent.md`)를 쓴다.
두 실행 파일은 동일한 **공통 원본** `docs/agents/uxui-agent.md`를 반드시
읽는다. 역할을 바꿀 때는 공통 원본만 수정하고 두 실행 파일에 복제하지 않는다.

이 에이전트에게 맡기는 일:
- 첫 화면·국면 선택·경기 화면·결과 화면의 정보 구조
- 375px 모바일 조작 동선과 44px 손가락 표적
- 색 대비·여백·반응형 레이아웃·가로 넘침 점검
- Open Football의 **정보 배치 원칙만** 참고한 독자적인 화면 개선

맡기지 않는 일: `src/sim/` 확률·밸런스, `src/render/` 축구 움직임, 실존 선수 데이터.

## 4.7 Coach 경기 분석 에이전트

경기 종료 뒤 득점·실점 원인과 사용자 판단을 설명하는 일은 Coach 에이전트가
관리한다. Codex에서는 **`coach_agent`** (`.codex/agents/coach-agent.toml`),
Claude Code에서는 **`coach-agent`** (`.claude/agents/coach-agent.md`)를 쓴다.
두 실행 파일은 공통 원본 `docs/agents/coach-agent.md`를 반드시 읽는다.

이 에이전트에게 맡기는 일:
- 득점·실점 장면의 주원인·보조원인과 확신도
- 포메이션·전술·교체·개별 지시가 결과에 준 영향
- 한 경기 결과와 150판 판단 확률을 분리한 감독 총평
- 다음 경기에서 무엇을 언제 바꿀지 구체적인 처방

맡기지 않는 일: 경기 결과·확률·난수 변경, 관전 연출 수정, 근거 없는 원인
추측. 분석에 필요한 기록이 없으면 `game-agent`에 읽기 전용 관찰 데이터
추가를 요청한다.

## 5. 지금 상태와 다음 할 일

진행 현황은 **[`docs/progress.md`](docs/progress.md)** 에 있다. 스무 단계 체크리스트와 현재 위치가 맨 위에 있다.

- 설계 명세 — [`docs/design.md`](docs/design.md)
- 무엇을 왜 그렇게 정했는지 — [`docs/worklog.md`](docs/worklog.md)
- Claude Code 시작 요약 — [`docs/CLAUDE_HANDOFF.md`](docs/CLAUDE_HANDOFF.md)

**결정의 근거가 `worklog.md`에 있다.** 폐기한 설계를 다시 제안하기 전에 거기부터 읽는다. 특히 "실제 감독 vs 나"와 "레버 순서 퍼즐"은 이유가 있어 버렸다.

---

## 6. 명령어

```bash
npm run dev     # 개발 서버
npm test        # 테스트 289개
npm run sim     # 밸런스 검증 (시드 1200개 x 27조합 x 3국면)
npm run build   # 프로덕션 빌드 (타입 검사 포함)
```
