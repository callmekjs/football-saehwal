# Codex 인수인계 프롬프트

> **이 파일이 다른 에이전트에게 넘기는 인수인계서다.**
> 아래 블록을 그대로 복사해서 Codex에 붙여넣거나, Codex에게
> "`docs/HANDOFF.md` 읽고 시작해"라고만 말해도 된다.
>
> **작업을 끝낸 에이전트는 아래 "지금까지 끝난 것 / 다음에 할 것"을 반드시
> 갱신한다.** 갱신하지 않으면 다음 사람이 이미 끝난 일을 다시 한다.
>
> 마지막 갱신: 2026-07-28 · 7/20 완료 (7번 관전 연출 — 공 물리·슛 판단·골키퍼)

---

```
축구 사활(football-saehwal) 프로젝트를 이어서 개발한다.
저장소: C:\football_hackerton  (https://github.com/callmekjs/football-saehwal)

## 시작 전 반드시 할 것

1. `git pull` 한다.
2. `AGENTS.md` 를 읽는다. 어기면 조용히 깨지는 규칙 세 가지가 있다.
3. `docs/progress.md` 로 현재 위치를 확인한다.
4. `docs/worklog.md` 로 무엇을 왜 그렇게 정했는지 확인한다.
   폐기된 설계를 다시 제안하기 전에 반드시 읽는다.
5. 작업이 끝나면 즉시 커밋·푸시한다. 커밋하지 않은 변경 위에 다른 작업을
   쌓지 않는다. 이 폴더는 다른 에이전트와 공유한다.
6. **작업을 끝내면 이 파일(`docs/HANDOFF.md`)의 "지금까지 끝난 것 / 다음에
   할 것" 목록을 갱신한다.** 이것이 다음 사람에게 넘기는 인수인계서다.
   갱신하지 않으면 다음 사람이 이미 끝난 일을 다시 한다.

## 이 프로젝트가 무엇인가

바둑 사활문제를 축구로 옮긴 실시간 전술 퍼즐 웹게임. 해커톤 출품작.
경기 15분 구간을 실시간 75초 안에 푼다. 시계는 멈추지 않고 되돌릴 수 없다.
플레이어는 감독으로서 포메이션 5종, 전술 레버 3축, 교체 카드를 쓴다.

마감 2026-08-03(월) 10:00. **그 이후 커밋 이력이 있으면 실격이다.**
실질 마감은 8/2(일) 밤.

## 지금까지 끝난 것 (7/20)

- [x] 1~3. 시뮬레이션 엔진 — 750틱 · 확률 모델 · 결정론
- [x] 4. 가상 선수 27명 (이름 없이 등번호만)
- [x] 5. 경고·퇴장·부상·페널티·교체 카드
- [x] 6. 자동 밸런스 검증 (`npm run sim`)
- [x] 7. 경기 화면 + 관전 연출 (공 소유·패스·압박·슛·골·킥오프)
- [x] 7-b. 관전 사실성 2차 — 아래 다섯 가지를 측정해서 찾아 고쳤다
      · 공이 사람 사이를 최대 31m 순간이동하던 것 (지금 최대 4.5m, 태클 범위)
      · 골 셋 중 하나가 화면에 안 나오고 점수판만 바뀌던 것 (지금 100%)
      · 공이 골라인 밖으로 나가도 골킥이 없던 것 (지금 판당 두세 번)
      · 대형이 볼 사이드로 접혀 공이 세로 폭의 62%만 쓰던 것 (지금 80%)
      · 반칙이 통계로만 있고 프리킥이 없던 것 · 골키퍼가 골문을 비우던 것
      · 경기가 킥오프 없이 시작하던 것
- [x] 7-c. 관전 사실성 3차 — **공에 물리를 넣고, 슛 판단과 골키퍼를 고쳤다**
      · 공이 목표 지점에 닿는 순간 단번에 서던 것 → 속도·마찰·튕김·체공
      · 슛 거리 중앙값 47m(최대 89m) → 15.5m(최대 32m). 자기 진영 슛 0회
      · 박스 안에서 패스 26 대 슛 7 → 패스 1 대 슛 14
      · 골키퍼가 상대 공격 중에 골라인에서 11m 나가 있던 것 → 5.9m,
        페널티 지역을 절대 안 넘는다 (전에는 최대 14.8m)
      · 반칙 휘슬이 골대로 향하던 득점 슛을 가로채 골이 사라지던 것 (어드밴티지)
      · 0.7초 간격 두 골이 서로를 덮어써서 하나가 사라지던 것 (예약을 줄로)
- [x] 7-d. 상태 표시를 **모양으로** 바꿨다. 사용자가 자기 화면의 표시를 못 읽었다
      · 빨강이 "상대 팀"과 "체력 고갈" 두 뜻, 노랑이 "경고"와 "수비라인" 두 뜻이었다
      · 경고 → 노란 카드(네모) · 퇴장 → 빨간 카드 · 부상 → 흰 바탕 빨간 십자
      · 체력 고갈 → 지친 얼굴 (이모티콘 아님. 13px 밑에서 뭉개지고 폰마다 다르다)
      · 수비라인은 팀 색과 안 겹치는 하늘색으로 옮겼다
      · 배지는 **선수 원 바깥에** 붙는다. 등번호를 가리면 누구를 교체할지 못 정한다
      · 부상·퇴장 선수가 화면에서 소리 없이 사라지던 것 → 3.4초/2.6초 쓰러져 있다
      · 아홉 명이 되면 **등번호 0번 유령**이 뛰던 것 (자리 수를 실제 인원으로 자름)
- [x] 7-e. **교체가 화면에 반영되지 않던 것**을 고쳤다
      · 재구성 조건이 "포메이션이 바뀌었나 / 인원이 줄었나"뿐이라 11→11 교체를 놓쳤다.
        실측으로 경기의 76%(4500프레임 중 3420) 동안 화면 명단이 시뮬과 어긋나 있었다
      · 자리를 배열 순서로 나눠줘서 한 명 바뀌면 뒤쪽 전원이 한 칸씩 밀렸다.
        이제 자리 번호가 선수에게 붙어 있어 뛰던 선수는 자리를 지킨다
      · 교체되는 순간 화면이 1.2초 멈추고, 나가는 선수는 터치라인으로 걸어 나가고
        들어오는 선수는 그 자리로 들어온다. **경기 시계는 안 멈춘다**

## 다음에 할 것 (8번부터)

- [ ] 8. **배포** — Vercel 등에 올려 접속 가능한 URL 만들기
- [ ] 9. 폰에서 실기기 확인 (교체가 손가락으로 되는지)
- [ ] 10. 경기 분석 화면 (정답 경로 vs 나, 기대값 델타, 감독 평점)
- [ ] 11. 코멘터리 문장 46줄
- [ ] 12. 국면 5개 완성 (현재 3개)
- [ ] 13. 밸런스 맞추기
- [ ] 14. 8/1 저녁 파라미터 동결
- [ ] 15. 최초 15초 온보딩
- [ ] 16~18. 폰 점검 · 버그 · 최종 배포

## 절대 어기면 안 되는 것

1. **`src/sim/` 은 브라우저를 모른다.** window·document·requestAnimationFrame·
   Date·Math.random 금지. 같은 코드가 Node에서 돌아야 밸런스 검증과 경기
   분석 화면이 산다. 확인: `npx tsx -e "import('./src/sim/engine.ts')"`
2. **난수 소비 순서를 바꾸지 않는다.** `src/sim/attack.ts` 의 `drawTick()` 은
   매 틱 정확히 18개를 정해진 순서로 뽑는다. 순서를 바꾸면 저장된 모든
   시드의 결과가 달라져 밸런스를 처음부터 다시 재야 한다.
3. **실존 선수 이름을 넣지 않는다.** 이 게임은 구조상 "가장 느린 수비수"를
   실점 원인으로 기계적으로 지목한다. 실명이 붙으면 대회 규칙(특정 선수
   비하 금지)에 걸린다. `Player` 타입에 name 칸 자체가 없다.

## 바꾸기 전후로 반드시 통과해야 하는 것

    npm test          # 170개
    npx tsc --noEmit  # 오류 0
    npm run sim       # 세 국면 전부 "합격"
                      # 11.0% / 47.0% / 38.2% 무개입 · 격차 21.5 / 24.0 / 26.1%p
                      # src/render/ 만 고쳤다면 이 숫자는 한 자리도 안 바뀐다.
                      # 바뀌었다면 시뮬을 건드린 것이니 되돌린다

`npm run sim` 이 미달을 내면 **`src/sim/constants.ts` 만** 조정한다.
구조를 바꾸지 않는다.

## 구조

    src/sim/      순수 함수 시뮬레이션. 확률로 결과만 정한다. 밸런스가 걸려 있다
    src/render/   관전 연출. 시뮬 결과를 축구 장면으로 옮긴다. 한 방향으로만 흐른다
      visual.ts   공 물리·소유·패스·압박·슛·골·킥오프·데드볼 재개. 경기 결과에 영향 없음
      pitch.ts    Canvas 그리기 전용 (공 높이 b.z 를 그림자와 크기로 표현)
    src/ui/       React 화면
    balance/      Node 전용 검증 하네스

## 작업 원칙

- 숫자를 추측으로 조정하지 말고 **먼저 측정한다.** 이 프로젝트에서 세 번
  추측으로 튜닝하다 실패했고, 매번 측정하니 원인이 다른 데 있었다.
- 테스트에 조정 대상 숫자를 박지 않는다. 방향과 비율로 쓴다.
- 기준값을 임의로 정하지 않는다. 실제 축구 값이나 명확한 근거를 쓴다.
- **희소한 사건은 여러 판을 합쳐서 잰다.** 슛은 한 판에 네다섯 번뿐이라
  세 판으로 거리별로 나누면 한 칸에 두세 개만 남아 통계가 되지 않는다.
  `visual.test.ts` 는 여섯 판(`SEEDS`)을 합산한다.
- 커밋 메시지는 한국어로, **왜** 그렇게 했는지에 지면을 쓴다.
- 작업 후 `docs/progress.md` 의 진행 표시를 갱신한다.

## 사용자에 대해

비전공자다. 결과 보고는 **전문용어 없이 다섯 줄로** 한다. 표나 긴 목록으로
늘어놓지 않는다. 더 물으면 그때 깊이 들어간다.
```

---

## 붙여넣은 뒤 이어서 할 말

Codex에게 무엇을 시킬지에 따라 아래 한 줄을 덧붙이세요.

| 시키고 싶은 것 | 덧붙일 문장 |
|---|---|
| 배포 | `8번 배포를 진행해줘. Vercel에 올리고 접속 URL을 알려줘.` |
| 경기 분석 화면 | `10번 경기 분석 화면을 만들어줘. 정답 경로와 내 선택을 나란히 재생하는 화면이다.` |
| 국면 추가 | `12번 국면 5개를 완성해줘. 현재 3개가 src/data/problems.json 에 있다.` |
| 코멘터리 | `11번 코멘터리 문장 46줄을 만들어줘.` |
| 버그 잡기 | `지금 상태에서 버그를 찾아 고쳐줘. 먼저 npm test 와 npm run sim 을 돌려봐.` |
| 축구가 어색함 | `.claude/agents/game-agent.md 를 읽고 그 기준으로 관전 연출을 점검해줘.` |

### 관전 연출에서 아직 남아 있는 것

| 남은 것 | 왜 안 고쳤나 |
|---|---|
| 골 셋 중 둘은 슛 없이 골망 장면으로 바로 넘어간다 | 시뮬은 확률로 득점을 정하는데, 그 순간 화면의 공이 반대편에 있을 때가 있다. 억지로 슛을 만들면 실측 80미터짜리 골이 나오고 골 장면이 득점보다 6초 늦었다. 중계처럼 골망으로 컷하는 쪽을 택했다. 근본 해결은 화면 쪽 공격이 상대 박스까지 도달하는 빈도를 올리는 것인데(현재 25m 안 점유 4.4%), 대형 로직을 크게 손봐야 한다 |
| 미드필더가 공격할 때 앞으로 다 못 나간다 | 목표 지점은 상대 박스 근처인데 실제로 도달하지 못한다. 소유가 빨리 바뀌어 수비라인이 목표보다 15m 뒤에 머문다 |
| **경기 화면에 범례가 없다** | 피치 위 표시(노란 카드·빨간 카드·흰 십자·지친 얼굴·하늘색 점선)가 무슨 뜻인지 알려주는 자리가 없다. 모양으로 뜻이 서게 만들었지만 처음 보는 사람에게는 한 줄 설명이 필요하다. **`src/ui/` 는 관전 연출 담당이 아니라 만들지 않았다.** 경기 화면 아래에 다섯 항목짜리 한 줄 범례를 붙이면 된다 |
| 데드볼이 경기의 12% | 슛이 늘면서 골킥·코너가 함께 늘었다. 상한(15%) 안이고 실제 축구도 이 정도다 |
| 오프사이드 | **만들지 않기로 했다.** 최종 수비수 라인 판정·부심 연출·플레이 되돌리기가 필요해 두세 시간이 든다. 실제 축구 빈도(90분 4회)를 이 화면의 15분으로 환산하면 판당 0.7회, 그마저 대부분 화면 밖에서 끝난다. 값어치가 낮다 |
