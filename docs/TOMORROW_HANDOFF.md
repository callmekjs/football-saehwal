# 내일 바로 이어서 할 것

> 작성 시각 **2026-08-01 밤** · 안전 커밋 `1cd45a5`
>
> 오늘 작업은 모두 `origin/main`에 푸시했고 작업 트리는 깨끗하다.
> **새 기능을 더 만들지 말고 아래 브라우저 재검증부터 시작한다.**

## 1. 첫 작업 — 권장안 시범 경기를 실제 브라우저로 다시 확인

오늘 실제 브라우저에서 권장안 관전이 70:00 `READY`에 멈추는 결함을 찾았다.
원인은 실제 앱의 `StrictMode` 두 번째 초기화가 첫 자동 시작을 덮은 것이고,
`1cd45a5`에서 고쳤다. 자동검사만 통과했으며 **수정 뒤 실브라우저 확인은 아직**이다.

`uxui_agent`를 깨끗한 HEAD에서 단독으로 실행한다.

1. p02 `잠긴 문` 후반을 한 판 끝낸다.
2. 결과에서 `이 권장안으로 경기 보기`를 누른다.
3. 70:00에서 즉시 시계가 흐르고 `4-3-3 · 라인 보통 · 압박 중 · 폭 넓게`가
   한 번 적용되는지 확인한다.
4. 포메이션·전술·교체·개별 지시·드래그가 모두 잠기는지 확인한다.
5. 시범 경기를 끝내고 원래 사용자 결과로 돌아오는지 확인한다.
6. 지난 기록의 개수와 저장 원문이 관전 전·중·후에 바뀌지 않는지 확인한다.
7. 전반부터 시작하는 판도 후반으로 자동 전환되는지 확인한다.
8. 1600×1000과 375×800에서 가로 넘침 0, 버튼 44px 이상, 콘솔 오류 0을 본다.

문제가 있으면 `game_agent`가 고치고 같은 UX 검사를 다시 한다. 문제가 없으면
코드를 고치지 말고 검증 결과만 보고한다.

## 2. 그다음 — final_eval 재평가

브라우저 재검증이 통과한 뒤 작업 트리를 깨끗하게 만들고 `final_eval`을 단독으로
실행한다. 코드를 고치는 에이전트와 동시에 돌리지 않는다.

- 사용자 경기 1판 → 권장안 시범 경기 1판 → 원 결과 복귀를 직접 완주
- 자동검사 742개, 타입, 빌드, 밸런스 다섯 국면을 직접 실행
- AI 문체 개선, 정보 밀도 축소, 전술 적용, 오프사이드 QA 결과를 확인
- 100점 만점 재채점, 80점 이상인지 판정

## 3. 오늘 끝낸 여섯 피드백

| 피드백 | 상태 | 근거 |
|---|---|---|
| AI가 만든 티 | 완료 | `12d3c36` · 한국어 문체 교정 |
| 전술이 적용되지 않음 | 미재현 | 같은 seed 300프레임에서 평균 전진 +11.30m, 폭 +7.18m |
| 잘못된 오프사이드 | 미재현 | `a0b4d7ec` · 10경기, 휘슬 11회, 오판 0 |
| 능력치가 달라 정답이 흐림 | 설명·기능 반영 | 같은 판 조건을 고정한 권장안 시범 경기 |
| 정보가 너무 많음 | 완료 | `09ea120` · 지난 기록 기본 문장 109→29 |
| 권장 전술 자동 시뮬레이션 | 구현 완료·브라우저 재검증 남음 | `2288508`, 자동 시작 수정 `1cd45a5` |

## 4. 시작 명령과 기준선

```bash
cd C:\football_hackerton
git pull --ff-only
git status                 # 깨끗해야 한다
git log --oneline -3       # 첫 줄 1cd45a5
npm test -- --run          # 742/742
npx tsc --noEmit           # 오류 0
npm run build              # 성공
npm run sim                # 다섯 국면 전부 합격
```

밸런스 기준선은 무개입 `10.7 / 48.0 / 46.8 / 37.0 / 19.5%`, 최선과의 격차
`20.5 / 22.6 / 27.6 / 28.0 / 26.3%p`다. 한 자리라도 달라지면 회귀다.

## 5. 제출 전에 사람이 해야 하는 것

- **시연영상은 아직 없다.** 공식 필수 제출물이므로 `docs/demo-video.md` 대본으로
  사람이 녹화하고 유튜브에 올려야 한다.
- 최종 마감은 **2026-08-03 10:00 KST**다. 그 뒤 커밋이 생기면 실격이다.
- final_eval 80점 이상을 다시 확인한 뒤 기존 GitHub 저장소를 Vercel에 Import한다.
- Vercel에서 새 저장소나 템플릿을 만들지 않는다.

