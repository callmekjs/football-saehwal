# Claude Code 인수인계

> Claude Code가 이 저장소를 처음 열거나 Codex의 작업을 이어받을 때 읽는
> 시작 문서다. 상세한 결정과 측정값은 `docs/HANDOFF.md`와
> `docs/worklog.md`가 단일 원본이다.
>
> 마지막 갱신: **2026-08-01 밤** · 84점이 짚은 **배포 전 두 건을 닫았다** ·
> 검사 678개
>
> 기능 기준: **이 문서를 포함한 최신 `main`**

## Claude에게 줄 한 줄

```text
docs/CLAUDE_HANDOFF.md를 끝까지 읽고, 안내된 원본 문서들을 확인한 뒤 이어서 작업해줘.
```

## 지금 당장 이어서 할 것

**`final_eval` 재채점이다.** 84점 판정이 감점 근거로 든 두 결함이 닫혔고,
작업 트리는 깨끗하다. 채점을 받을 때는 고치는 에이전트를 같이 돌리지
않는다 — 대상이 움직이면 그 점수가 무엇에 대한 점수인지 알 수 없다.

그 다음이 사용자 동의를 받은 Vercel 배포다. 절차는 `HANDOFF.md` 에
확정돼 있고, **저장소에 커밋을 만들지 않는 Import 경로만** 쓴다.

### 8/1 밤에 닫은 두 건

**① 보고서 화면의 무한 저장 루프.** 보고서를 띄워 두면 같은 판이 약
60초마다 한 건씩 다시 저장되고 150판 비교도 다시 돌았다(공개본 3분 관찰에서
기록 1 → 4건).

원인은 `App.tsx` 가 `if (picked)` 안에서 경기용 `problem` 객체를 **매 렌더
새로 만든 것**이었다. 그 객체가 `AnalysisPanel` 의 효과 의존성까지
내려가는데 참조가 매번 달라지므로 효과가 매번 다시 돌았고, 효과가 150판을
돌린 뒤 `onDelta → onFinish → setHistory` 로 App 을 다시 그리면 또 새 객체가
생겼다. 한 바퀴가 곧 150판이라 주기가 60초였다.

세 겹으로 막았다 — App 의 `useMemo`, `AnalysisPanel` 의 값 기반 의존성과
「이미 끝낸 판」 빗장, `MatchScreen` 의 `onDelta → onFinish` 빗장.
`SAME_MATCH_WINDOW_MS`(60초)와 `ANALYSIS_RUNS`(150)는 안 건드렸다.

★ **빗장을 「붙는 순간 한 번」으로 걸면 안 된다.** 앱은 `StrictMode` 안에서
돌아 효과를 붙였다 떼었다 다시 붙인다. 그러면 두 번째가 빗장에 걸려
분석이 아예 안 돌고 화면이 영원히 「검증 중…」에 머문다. 「끝냈는가」로
걸고, 끝나기 전에 떼어지면 풀어준다.

**② 배치판 카드로 교체가 안 되던 것.** 벤치의 「고른 선수」 상태가 벤치
패널 안에만 있어서 옆의 배치판은 그것을 몰랐다. `MatchScreen` 으로 끌어올려
두 패널이 같은 것을 본다. 들어올 선수가 정해진 동안에는 배치판 클릭이
**나갈 선수**가 되고, 그 동안은 카드를 끌 수 없다(손 떨림이 클릭을 자리
옮기기로 바꿔 삼켜 버린다). 평소에는 지금까지처럼 선수 상세가 열린다.
두 단계 탭·급수 타임 즉시·경기 중 6초·카드 차감은 그대로다.

**회귀검사 여덟 개**를 함께 붙였다(`AnalysisPanel.test.tsx` 셋,
`matchSubstitution.test.tsx` 다섯). 이 결함들이 커밋을 통과한 이유가
검사 0건이었다.

★ **검사에 `jsdom` 이 새로 들어갔다.** 지금까지 화면 검사는 전부
`renderToStaticMarkup` 이라 한 번 그려진 마크업만 봤고, 위 두 결함은
원리적으로 그것으로 안 잡힌다. 브라우저를 켜는 것은 그 두 검사 파일뿐이고
(`@vitest-environment jsdom`), 나머지와 `src/sim/` 은 지금까지처럼 Node 에서
돈다. 실제로 마운트하고 눌러 보는 도구는 `src/ui/domHarness.tsx` 다.

### 8/1 밤 — 공이 물리법칙대로 죽는다

사용자가 화면을 보고 지적했다 — *"공이 계속 같은 속도로 굴러가."*

마찰 코드는 있었고 **모양이 틀렸다.** 공기 항력은 속도의 **제곱**인데
속도에 **비례**하게 적어 두어서 초속 20미터 공이 1초에 12%밖에 안 잃었다
(실제 31%). 느린 공은 맞게 죽고 빠른 공만 등속으로 굴러갔다.

구름 저항(일정)과 공기 항력(v²)을 나눠 실제 값으로 적었다. 초속 16미터
공이 서기까지 **85m → 56m**(경기장이 105m다). 감속이 실제 축구 대비
0.59~0.91배 → **0.95~1.18배**다. 골 장면 100%·패스 77.7%·소유 37.8%로
관전 품질은 그대로고 `src/sim/` 은 안 열었다.

★ **검사가 못 잡은 이유** — 「마찰로 느려진다」가 **방향만** 봐서 실제의
5분의 3만 감속해도 통과했다. 이제 빠른 공과 느린 공의 감속 **비율**을
잰다(제곱 4.7배 · 비례 2.5배 · 요구 3.2배).

작업 중 **검사 두 개의 전제가 낡아 있던 것**도 고쳤다 — 오프사이드 빈도가
한 판을 15분으로 보고 있었고(실제 22분), 골 거리 검사가 양 팀이 2초 안에
넣으면 상대의 슛을 잡아 유령 골을 보고했다. 자세한 것은 `HANDOFF.md` 에
있다. **뜬 공(`AIR_DRAG`)은 아직 옛 모양이고 일부러 남겼다.**

### 8/1 밤 — 반칙에 대형이 서고, 하프타임에 진영을 바꾼다

사용자가 둘을 정했다 — *"심판이 반칙을 선언하면 모든 선수들은 멈추고
자신의 진형쪽으로 가야한다"*, *"전반전 급수 타임을 했으면 후반전
넘어갈때 반대진형에서 시작하는거 (실제 축구처럼)."*

**반칙 뒤 대형 복귀** — 0.9초 전원 정지는 그대로 두고, 그 **다음**을
바꿨다. 전에는 곧바로 **공 기준** 목표를 다시 쫓아서 휘슬이 불렸는데도
스물두 명이 공 쪽으로 모여들었다. 이제 각자 대형 자리(`homeX/homeY`)로
걸어 돌아간다. 실측으로 프리킥 동안 자기 자리에 가까워지는 프레임이
**54.4% → 76.9%** 다. 휘슬이 없는 재개(스로인·골킥·코너킥)에는 안 건다.

★ **대형을 「얼리는」 것과 혼동하지 마라.** 얼리는 것은 이미 해봤고
되돌렸다(박스 안 판단 36판에 7회 → 0회). 돌아가는 것은 그 반대다.

**하프타임 진영 교대** — 계산은 언제나 같은 방향으로 하고 `drawPitch` 가
좌우만 뒤집는다. 조건은 `half === 2 && firstHalf !== null` 이라 후반만
골라 시작한 판은 그대로 그린다. 브라우저에서 전반을 뛰고 넘어가
`flipped: true, half: 2` 를 확인했다.

★ 뒤집기를 넣다가 **잠복 버그**가 드러났다 — 자리를 옮기는 `X()` 로
중앙 원 반지름과 박스 너비를 재고 있었다(뒤집으면 9.15m 가 95.85m 가
된다). 길이 전용 `L()` 과 사각형 전용 `rect()` 로 갈랐다. 스로인이
32.2m 를 날아가던 것(`THROW_MAX` 없음)도 함께 고쳤다.

### 남은 것

1. **`final_eval` 재채점** — 깨끗한 트리에서
2. Vercel 배포 (사용자 동의 필요)
3. `HANDOFF.md` 「할 일 넷」의 ③(「지침」 표현, `korean`)과
   ④(기준팀 통과율 재측정, 80분, 배포 뒤)
4. 급수 타임·후반 교체가 「교체됨」이 아니라 「선발 제외」로 보이는 라벨
   문제 — 이번 변경과 무관한 예전 동작이고 `HANDOFF.md` ②에 적어 뒀다

## 가장 먼저 알아야 할 것

- 실제 저장소는 **`C:\football_hackerton`** 이다.
- 원격 저장소는 `https://github.com/callmekjs/football-saehwal` 이다.
- OneDrive 아래의 예전 작업 폴더는 사용하지 않는다. 동기화를 일부러 껐고,
  파일 감시와 개발 서버가 멈추는 원인이 될 수 있어 로컬 저장소로 옮겼다.
- 사용자는 Claude Code와 Codex의 토큰을 번갈아 쓸 예정이다. 두 도구는 같은
  저장소를 공유하므로 **한쪽의 미커밋 변경 위에 다른 쪽 작업을 쌓지 않는다.**
- 설치형 모바일 앱이 아니라 **데스크톱 중심의 동적 웹서비스**다.
- 사용자는 비전공자다. 완료 보고는 전문용어 없이 결론 다섯 줄로 쓴다.

## 작업을 시작하는 순서

```bash
cd C:\football_hackerton
git status
git pull --ff-only
```

그다음 아래 문서를 순서대로 읽는다.

1. `AGENTS.md` — 공유 작업·커밋·불변식
2. `docs/defects.md` — **지금 무엇이 고장나 있나.** 열린 결함과 다시
   시도하면 안 되는 것이 한 곳에 있다
3. `docs/HANDOFF.md` — 지금까지 끝난 것과 다음 할 일
4. `docs/progress.md` — 일정과 현재 위치
5. `docs/worklog.md` — 설계 결정과 폐기한 접근
6. 맡은 역할의 에이전트 파일

작업 트리가 더러우면 다른 에이전트가 작업 중일 수 있다. 그 파일을 수정하거나
되돌리지 말고 사용자에게 먼저 알린다.

## 프로젝트 한 문장

바둑의 사활 문제를 축구에 옮긴 실시간 감독 시뮬레이션이다. 한 국면의 경기
22분을 75초에 압축하고, 사용자가 포메이션·전술·선수 지시·교체를 선택한 뒤
종료 화면에서 더 나은 전술과 그 근거를 배운다.

사용자가 정한 표현:

- 제품을 **시뮬레이션**이라고 부른다.
- **퍼즐**이라고 부르지 않는다.
- 핵심은 제한 시간 안의 다양한 감독 개입과 경기 후 전술 학습이다.

## 지금까지 완료한 것

### 1. 결정론적 시뮬레이션 엔진

- 750틱, 한 틱 0.1초, 한 경기 75초
- 포메이션·라인·압박·폭·선수 상태·교체·카드·부상·페널티 반영
- 가상 선수 27명, 실명과 실제 구단 데이터 없음
- 매 틱 난수 18개를 고정 순서로 소비
- Node와 브라우저에서 같은 결과

### 2. 감독 조작

- 포메이션 9종
- 전술 프리셋 5종: 균형·역습·측면 공략·전방 압박·밀집 수비
- 라인·압박·폭 세부 레버
- 개별 선수 지시 5종
- 교체 카드와 6초 반영 지연
- 우리 팀 배치판의 44px 선수 카드를 원하는 좌표로 자유 드래그
- 가로·세로·대각선 드래그를 모두 실제 피치 좌표로 기록
- 마우스가 없으면 수비·중원·공격 × 왼쪽·중앙·오른쪽 3×3 버튼과
  `기본 자리` 버튼으로 같은 기능 사용
- 경기장 캔버스의 7px 선수 원은 계속 드래그하지 않음
- 교체는 계속 두 단계 탭으로 조작

### 3. 관전 연출

- 공 소유·패스·인터셉트·태클·슛·선방·골망·세리머니
- 킥오프·프리킥·스로인·골킥·코너킥
- 가속·감속·체공·튕김·마찰
- 교체·부상·퇴장·경고·체력 표시
- 시뮬 시계를 따라잡아 탭 전환이나 느린 프레임이 일시정지가 되지 않음
- 시뮬이 골을 정해도 실제 공격과 슛 장면이 만들어진 뒤 점수판이 올라감
- **주심 하나와 부심 둘.** 부심이 오프사이드 라인(뒤에서 두 번째 수비수)에
  서고, 둘이 서로 반대편 터치라인의 반대편 절반을 맡는다
- **오프사이드** — 규칙 11조대로다. 깃발이 먼저 올라가고, **그 선수가
  공을 건드리면** 휘슬이 분다. 위치에 있는 것만으로는 반칙이 아니라서
  수비수가 먼저 끊으면 깃발을 내린다(실측 깃발의 53.8%). 골킥·스로인·
  코너킥을 직접 받는 공은 예외이고 프리킥·킥오프는 예외가 아니다. 판당
  0.75회로 실제 축구(0.7~1.0회)와 같은 자리다. 페널티 지역 안도 본다
- **주심이 판정을 준다** — 반칙·페널티킥·오프사이드·킥오프는 주심 휘슬,
  스로인·코너킥·골킥은 부심 깃발이다. 경고·퇴장은 주심이 선수 앞까지 가서
  손에 카드를 든다. 실제 축구에서 스로인마다 휘슬이 울리지 않는다
- **오프사이드 라인이 실선으로 그려진다.** 수비하는 팀 색이고, 그 팀의
  뒤에서 두 번째 수비수를 지난다. 부심이 그 선 위에 서 있다. 하늘색
  점선(감독이 설정한 수비라인)과는 실선/점선·색으로 구분된다
- **그리기 자체를 검사하는 테스트가 생겼다**(`src/render/pitch.test.ts`).
  가짜 캔버스에 그려서 무엇을 어디에 어떤 색으로 그렸는지 확인한다.
  좌표 계산이 다 맞아도 화면 밖에 그리면 소용없다는 것을 실제로 겪었다

최근 수정:

- 공이 아웃되려는 중 예약 골이 만료되어 갑자기 실점으로 바뀌던 충돌을
  수정했다.
- 공이 0.5초 안에 라인을 벗어날 궤적이거나 아웃 재개 중이면 예약 골을
  실행하지 않는다.
- 720경기·886개 골 장면에서 세리머니와 아웃 재개의 동시 발생은 0개다.

### 4. UX/UI

- 데스크톱: 우리 팀·큰 경기장·상대 및 상황을 나눈 3열
- 휴대폰 웹: 점수·상황·피치·조작 탭 순서
- 전술·포메이션·선수·기록 중 한 영역만 열어 정보 과부하를 줄임
- 375×812에서 가로 넘침 0px, 버튼 최소 높이 44px
- 홈 킥오프 허브: 13개 상대, 큰 경기 프리뷰, HUD 4칸, 국면 5장,
  전·후반 선택과 단일 KICK OFF
- 국면 카드에 미국 기준 무개입 통과율을 공개하고, 가짜 선수 평점 대신
  다음 판과 같은 시드의 실제 체력을 표시
- `ZOXEXIVO/open-football`은 큰 피치와 정보 계층의 원칙만 참고했다.
  코드·컴포넌트·CSS·색·문구·이미지·선수 데이터는 복사하지 않았다.

### 5. 경기 종료 Coach 분석

- 무개입·사용자 판단·권장 전술을 같은 시드로 150경기씩 비교
- 한 경기의 운과 판단의 품질을 분리
- 득점·실점 시각, 발생 경로, 당시 전술, 확신도와 근거 표시
- 150경기 평균 득점·실점·슈팅·세트피스·배후 침투 비교
- 감독 총평 5줄, 전환점, 결정 평가와 다음 경기 처방 제공
- 맨 앞의 「이 경기의 한 수」가 실제 결정 하나의 시각과 내용을 크게 표시
- 여러 결정의 ±%p는 한 수의 단독 효과가 아니라 전체 판단 묶음이라고 명시
- 개입하지 않은 경기는 존재하지 않은 한 수를 만들지 않고 `개입 없음` 표시
- 기록에 없는 슈터·도움·xG·패스 경로는 추측하지 않음

### 6. 검증된 국면 5개 — 전부 전반·후반을 갖는다

**모든 국면이 전반판과 후반판을 함께 갖고 플레이어가 고른다.** 국면
카드에 버튼이 둘이다.

```
전반   22:00 급수 타임 ─ 3분 ─▶ 25:00 재개 ─ 22분 ─▶ 47:00 전반 종료
후반   67:00 급수 타임 ─ 3분 ─▶ 70:00 재개 ─ 22분 ─▶ 92:00 경기 종료
```

- 시작 분과 반은 **국면 데이터에 없다.** 같은 국면이 두 시각을 가지므로
  적을 수 없다. 단일 원본은 **`src/matchClock.ts`** 이고, **거기 밖에서
  45·90·22를 직접 쓰지 마라.**
- 경기 길이는 언제나 750틱이다. 이 파일은 그 750틱에 몇 분이라고 이름을
  붙일지만 정하므로 **밸런스와 무관하다.**
- 전반을 고르면 종료 화면에 **하프타임 주장 정리**가 붙는다
  (`src/analysis/halftime.ts`). 실제 기록만 말하고 명령하지 않는다.
- 전반이 끝난 것은 **"전반 종료"** 이지 경기 종료가 아니다.

### 6-2. 상대 팀 선택 — 2026-07-30 (난이도 3단계를 대체했다)

킥오프 전에 오늘 만날 상대를 고른다. **FIFA 순위를 참고한 실제 국가 열세
팀**이며 팀마다 하는 축구가 다르다. 쉬움·보통·어려움 3단계는 **없어졌다** —
세기만 다른 같은 팀이라 무엇을 골라도 대응이 똑같았기 때문이다.

- 단일 원본은 `src/sim/constants.ts` 의 `OPPONENTS` 이고, 화면·주장·
  보고서가 `src/analysis/opponents.ts` 를 통해 그 값을 읽는다.
- **선수 개인 이름은 여전히 넣지 않는다.** 불변식 3번은 팀이 아니라
  선수에 대한 것이다 — 실점 공식이 가장 느린 수비수를 기계적으로 지목하기
  때문이다. 엠블럼·유니폼 이미지도 저작권 때문에 안 쓴다.
- 난수를 하나도 더 안 쓴다. 순수 함수 `applyOpponent` 가 이미 뽑아둔
  난수와 비교되는 계수만 곱한다.
- **기준팀은 미국(16위)이고 계수가 전부 1.0이라 완전한 항등이다.**
  다섯 국면의 합격 기준선은 언제나 기준팀에서만 잰다.
- 세기를 **공격력과 수비력으로 나눈 것**이 이 표의 핵심이다. 눈금 하나만
  두면 아래 순위 팀이 위협도 적고 뚫기도 쉬워서 지키는 국면이 사라진다.
- `shape` 는 위협의 크기가 아니라 **모양**이다. 브라질은 배후 비중이
  스페인의 두 배가 넘어 라인을 올리면 뚫린다. 팀마다 답이 다르다.
- `npm run sim` 은 기준팀만, `npm run sim -- --team=ALL` 이 열세 팀을 잰다.

### 6-1. 국면 상세

- 다섯 번째는 `p03 · 경고가 쌓였다`
- 1-0 리드에서 3-4-3 · 라인 낮음 · 압박 강 · 폭 좁게로 시작
- 권장: 5-4-1 · 라인 보통 · 압박 중 · 폭 넓게
- 1,200시드에서 무개입 48.1% → 최선 72.8%, 격차 24.7%p
- 탈락한 「욕심의 값」 원안은 다시 넣지 않음

### 7. 선수 자유 위치 배치

이번 Codex 작업의 핵심이다. 사용자가 기존 드래그를 다시 문제 삼았고,
QA 결과 단순 버그가 아니라 **요구를 잘못 정의한 것**으로 확인됐다.

수정 전:

- 위로 끌면 `PUSH_UP`, 아래로 끌면 `DROP_BACK`
- 가로·빈 공간·같은 줄 이동은 원위치 또는 화면 전용 자리 교환
- 전용검사 28개와 이전 QA도 이 제한을 정상 동작으로 고정
- 따라서 검사에 모두 통과해도 “원하는 위치에 놓기”는 존재하지 않았음

수정 후:

- `PlayerPosition { x, y }`, `PlayerState.position`,
  `POSITION { target, position }` 결정 추가
- x는 우리 골문 0 → 상대 골문 105, y는 터치라인 0 → 68
- 드롭 좌표가 실시간 상태·결정 재현·중앙 경기장·종료 뒤 150판 분석에
  같은 값으로 반영
- 카드가 겹치면 44px 표적을 보존하는 가까운 빈자리로만 최소 보정
- 골키퍼·피치 밖·수비 셋 미만은 `checkPosition()` 한 곳에서 차단
- 새 포메이션을 고르면 모든 자유 좌표를 `null`로 만들고 전체 재배치
- 교체로 나가거나 들어오는 선수도 자유 좌표 초기화
- 직접 위치는 `PUSH_UP`·`DROP_BACK`을 풀고, 둘을 새로 걸면 자유 위치 삭제
- `HOLD`·`BACK_OFF`·`CONSERVE`는 자유 위치와 함께 유지 가능

자유 위치는 화면 장식이 아니다.

- 전진하거나 측면으로 벌리면 우리 공격 기회와 상대 역습 위험이 함께 증가
- 후퇴하거나 중앙을 채우면 둘이 함께 감소
- 한 선수 극단 배치 약 4%, 여러 선수 누적 24% 상한
- 직접 배치가 0명이면 기존 계수 객체를 그대로 반환해 저장된 결과와
  매 틱 난수 18개 소비 순서를 보존
- 숫자 단일 원본은 `src/sim/constants.ts`의 `FREE_POSITION`

주요 파일:

- 계약·검증·재현: `src/sim/types.ts`, `src/sim/engine.ts`
- 확률 영향: `src/sim/tactics.ts`, `src/sim/constants.ts`
- 중앙 경기장: `src/render/visual.ts`
- 드롭 좌표·충돌: `src/ui/squadDrag.ts`
- 화면·3×3 버튼: `src/ui/SquadPanel.tsx`
- 실시간 결정 기록: `src/ui/useMatch.ts`
- 종료 분석: `src/analysis/coach.ts`

실제 브라우저 QA:

- 7번 가로 드래그 → `중원 · 오른쪽`
- 8번 대각선 드래그 → `공격 · 왼쪽`
- 6번 세로 드래그 → 별도 좌표
- 세 명 모두 `배치 3`으로 기록
- `5-4-1` 선택 → `배치 0`, 새 대형으로 재배치
- 3×3 + 기본 자리 버튼 높이 44px
- 1280px 가로 넘침 0, 깨끗한 새 탭 오류·경고 0
- 상세 기록은 `docs/qa-log.md`의 `QA-06`

## 에이전트 역할 — 넷 다 Codex·Claude Code 양쪽에서 쓴다

| 에이전트 | 역할 | 코드를 고치나 |
|---|---|---|
| 게임 | 축구 규칙·엔진·관전 연출·밸런스 | 고친다 |
| QA | 브라우저로 끝까지 검사하고 숫자로 보고 | **절대 안 고친다** |
| UX/UI | 웹 정보 구조·조작·반응형 | UI만 고친다 |
| Coach | 경기 종료 원인 분석과 처방 | 분석 영역만 고친다 |

**지침의 단일 원본은 `docs/agents/<이름>.md` 하나다.** 플랫폼별 실행
파일은 그 원본을 읽으라고만 적혀 있는 얇은 파일이다.

| | Claude Code | Codex |
|---|---|---|
| 실행 파일 | `.claude/agents/<이름>.md` | `.codex/agents/<이름>.toml` |
| 부르는 이름 | `game-agent` 처럼 하이픈 | `game_agent` 처럼 밑줄 |

**역할을 바꿀 때는 `docs/agents/` 의 공통 원본만 고친다.** 실행 파일에
내용을 복사하면 한쪽만 수정되어 서로 다른 에이전트가 된다.

동시에 여러 개를 돌릴 때 주의: **QA 는 코드를 안 고치므로 언제나 안전**
하지만, 게임과 UX/UI 는 둘 다 화면을 만질 수 있어 같이 돌리면 충돌한다.

## 절대 바꾸면 안 되는 불변식

1. `src/sim/`에서 브라우저 API·`Date`·`Math.random`을 사용하지 않는다.
2. `drawTick()`의 난수 18개와 순서를 바꾸지 않는다.
3. 실존 선수 이름·사진·구단 데이터를 넣지 않는다.
4. 확률 숫자는 `src/sim/constants.ts`에만 둔다.
5. 관전 연출이 시뮬 결과를 바꾸지 않는다.
6. 국면 추가는 `src/data/problems.json`만으로 가능해야 한다.
7. 작동하지 않는 버튼이나 휴대폰에서 누를 수 없는 조작을 만들지 않는다.

## 현재 검증 기준

```bash
npm test
npx tsc --noEmit
npm run sim
npm run build
```

- 테스트: **678개 모두 통과**
- 무개입 통과율(**기준팀 미국**):
  **10.7% / 48.0% / 46.8% / 37.0% / 19.5%**
- 최선과의 격차(**기준팀 미국**):
  **20.5%p / 22.6%p / 27.6%p / 28.0%p / 26.3%p**
- **「길이 막혔다」의 최선 격차가 합격선보다 0.5%p 높을 뿐이다.**
  다음 시뮬레이션 변경은 이 국면을 가장 먼저 확인한다
- 다섯 국면 모두 합격
- 13개 팀도 각각 1,200시드로 실행했고 13개 명령이 모두 정상 종료했다.
  비기준팀은 참고용이라 합격 판정하지 않는다. 베트남 상대 「잠긴 문」과
  「경고가 쌓였다」 무개입은 64.7%·64.8%로 기존 60% 위험선을 넘었다.
- 관전 코드만 바꿔도 위 열 숫자가 한 자리라도 움직이면 회귀다.
- **위 숫자는 계수가 전부 1.0인 미국의 값이다.** 다른 12개 팀은 공격과
  수비 성향이 달라 결과와 최선 판단이 바뀐다.

## 아직 해야 할 것

**GitHub Pages 작업용 배포는 끝났다** —
<https://callmekjs.github.io/football-saehwal/> 이고 `main` 에 push 하면
자동으로 다시 올라간다. Vercel 최종 배포는 이 문서 맨 위 순서대로 남아 있다.

**시연영상은 사용자가 "중요하지 않다"고 정했다. 더 권하지 마라.**

**QA-15·QA-16 은 7/31 밤에 고쳤다**(커밋 `28fbbcb`). 배후 실점 장면이
계산상 원인인 그 수비수를 지나는 비율이 21.3% → 93.9%, 패스 성공률이
59.3% → 72.6% 다. QA 10회차 표에 이 둘이 「미수정」으로 적혀 있는데,
그 회차는 수정 **전** 커밋 스냅샷을 쟀다. **다시 고치지 마라.**

남은 것은 QA 열 회차가 찾은 서른두 건 중 열다섯 건이다. 순서와 근거는
**`docs/HANDOFF.md` 의 「다음에 할 것」** 에 표로 있다. 가장 먼저 볼 것만
적으면:

1. **QA-28** 같은 0.1초에 양 팀 득점 또는 반칙과 득점을 함께 확정한다
2. **QA-17** 퇴장한 선수를 예약 교체가 대신해 인원이 되살아난다
3. **QA-26** 공 전체가 골라인을 넘었는데 선방·코너로 처리한다

**QA-31은 수정됐다.** 20초·75초 복귀를 각각 200시드로 재현했고, 정상
재생과 다른 최종 상태·결정 기록은 모두 0건이었다.

그 밖에 알려진 것 — 개별 지시 다섯 종은 모두 적어도 한 국면에서 노이즈
바닥을 넘었고, 국면마다 이로운 지시가 갈려 만능 답은 없었다.
미국(16위)과 일본(17위)의 평균 능력치 순서는 역전돼 있다(미국이 기준팀이라
계수가 0 이어서다).

**오늘 바뀐 것의 전체 목록은 `docs/HANDOFF.md` 맨 위 절에 있다.**

다음 작업자는 자유 배치를 다시 `PUSH_UP`·`DROP_BACK` 두 개로 축소하거나
화면 전용 자리 교환을 복구하면 안 된다. 기능 변경 전
`docs/worklog.md`의 “상하 지시 드래그를 실제 자유 좌표로 바꿨다”와
`docs/qa-log.md`의 QA-06을 먼저 읽는다.

알고 있지만 미룬 축구 사실성 문제와 측정값은 `docs/HANDOFF.md`의
“아직 축구로 안 보이는 것”과 “알면서 안 고친 것”을 읽는다. 이미 해결된
문제를 다시 보고하거나 근거 없이 튜닝하지 않는다.

## 커밋과 종료 절차

IDE에서 **Changes**와 **Staged Changes**를 구분해 확인하고, 필요한 파일만
스테이징한다. 커밋 메시지는 `AGENTS.md`의 규칙을 따른다.

```text
type: 명령문 제목

무엇을 바꿨는지와 왜 필요한지를 설명하는 본문.
본문은 영문 기준 72자마다 줄바꿈한다.
```

사용 유형: `feat`, `fix`, `docs`, `style`, `refactor`, `test`, `chore`

끝나면 반드시:

1. 관련 검사 실행
2. `docs/progress.md` 갱신
3. `docs/HANDOFF.md` 갱신
4. 필요하면 이 문서도 갱신
5. 한국어 제목의 규칙형 커밋 작성
6. 즉시 푸시

## 현재 브라우저

마지막 확인 때 기존 `http://localhost:5177/`은 연결되지 않았다. 로컬 서버가
살아 있다고 가정하지 말고 먼저 확인한다. 실행 중인 서버가 있으면 중복으로
띄우지 않는다. 없고 실제 브라우저 검증이 필요할 때만 사용자와 합의한 포트로
하나를 띄운다. 작업용 배포본은 아래 주소다.

<https://callmekjs.github.io/football-saehwal/>
